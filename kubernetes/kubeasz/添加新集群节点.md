# node节点安装python3
```shell
# 先上传文件夹内的python3.tar.gz
tar -xzvf python3.tar.gz -C /usr/local/
echo 'export PATH="/usr/local/python3/bin:${PATH}"' > /etc/profile.d/python3.sh
source /etc/profile
python3 -m pip install --upgrade pip
mkdir -p ~/.pip
cat << EOF > ~/.pip/pip.conf
[global]
index-url = http://mirrors.aliyun.com/pypi/simple/

[install]
trusted-host = mirrors.aliyun.com
EOF
ln -s /usr/local/python3/bin/python3 /usr/bin/python3
```
# 在kubeasz所在节点执行

```shell
# 查询集群名称 -> default
[root@easzlab kubeasz]# ls -al /etc/kubeasz/clusters

# ssh 免密码登录，增加节点为 192.168.235.129
[root@easzlab kubeasz]# ssh-copy-id 192.168.235.129

# 新增节点并自定义nodename，新增的节点上面一定要安装python3
[root@easzlab kubeasz]# pip install ansible
[root@easzlab kubeasz]# cd /etc/kubeasz
[root@easzlab kubeasz]# ./ezctl add-node default 192.168.235.129 k8s_nodename=worker-01

[root@easzlab kubeasz]# kubectl get node -o wide -A
NAME              STATUS   ROLES    AGE   VERSION   INTERNAL-IP       EXTERNAL-IP   OS-IMAGE                KERNEL-VERSION                CONTAINER-RUNTIME
192.168.235.128   Ready    master   37d   v1.28.1   192.168.235.128   <none>        CentOS Linux 7 (Core)   5.4.278-1.el7.elrepo.x86_64   containerd://1.6.23
worker-01         Ready    node     29s   v1.28.1   192.168.235.129   <none>        CentOS Linux 7 (Core)   5.4.278-1.el7.elrepo.x86_64   containerd://1.6.23

# 删除集群（测试用，不用执行）
#./ezctl del-node default 192.168.235.129
```

