version: '3.8'
name: nexus3

services:
  nexus3:
    image: sonatype/nexus3
    container_name: nexus3
    restart: unless-stopped # 设置重启策略
    ports:
      - "8081:8081" # 映射主机端口到容器端口
    volumes:
      - ./data:/nexus-data # 持久化数据卷
